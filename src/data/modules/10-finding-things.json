{
  "id": "10-finding-things",
  "title": "10. Finding Things",
  "level": "Basic",
  "content": "# 10. Finding Things\n\nEfficient CLI work often means locating files, commands, processes, or text before acting on them.\n\n## find\n\n```bash\nfind . -type f -name '*.log'\nfind /var/log -type f -mtime -1\nfind . -type f -size +100M\n```\n\nUseful predicates include `-type`, `-name`, `-path`, `-mtime`, `-size`, `-user`, and `-perm`.\n\n## Execute safely\n\n```bash\nfind . -type f -name '*.tmp' -print\n```\n\nInspect results before adding actions such as deletion.\n\nFor arbitrary filenames, prefer NUL separation:\n\n```bash\nfind . -type f -print0\n```\n\nThen consume with a NUL-aware tool:\n\n```bash\nfind . -type f -print0 | xargs -0 wc -l\n```\n\n## Locate commands\n\n```bash\ncommand -v python\ntype -a python\n```\n\n`which` is less reliable as a teaching primitive because shell aliases/functions and built-ins can complicate command lookup.\n\n## Search file contents\n\n```bash\ngrep -R --line-number 'timeout' .\nrg 'timeout' .\n```\n\n`rg` (ripgrep) is often faster and more ergonomic for source trees.\n\n## locate\n\nIf installed:\n\n```bash\nlocate nginx.conf\n```\n\n`locate` uses an index, so results can be stale. `find` searches the filesystem directly.\n\n## File metadata\n\n```bash\nstat file.txt\nfile file.txt\n```\n\n`file` examines content/signatures; extensions are not authoritative.\n\n## Finding processes\n\n```bash\npgrep -af nginx\npidof nginx\n```\n\nThese are often cleaner than parsing `ps` output.\n\n## Practice\n\nFind all files larger than 10 MB under a practice directory, identify which are regular files, and calculate their total size. Do not delete anything until you have reviewed the list.\n"
}
